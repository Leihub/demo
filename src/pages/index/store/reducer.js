
const initState = {
    tieList:[]
}

const reducer = (state=initState,action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}